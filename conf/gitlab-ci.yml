variables:
  GIT_CLEAN_FLAGS: -ffdx -e node_modules/ -e vendor/ -e yarn.lock -e composer.lock

stages:
  - code_style
  - code_quality
  - test_unit
  - build
  - deploy
  - test_function

cache:
  # untracked: true
  key: "$CI_PROJECT_PATH_SLUG"
  paths:
    - vendor/
    - composer.lock
    - node_modules/
    - yarn.lock

# code_style:
#   stage: code_style
#   script:
#     - bash $HOME/runner/deploy.sh --code-style
#   only:
#     - develop

# code_quality:
#   stage: code_quality
#   script:
#     - bash $HOME/runner/deploy.sh --code-quality
#   only:
#     - develop
#   when: manual
#   allow_failure: true

# test_unit:
#   stage: test_unit
#   script:
#     - bash $HOME/runner/deploy.sh --test-unit
#   # when: manual
#   only:
#     - develop

# build:
#   stage: build
#   script:
#     - bash $HOME/runner/deploy.sh --build-php
#     - bash $HOME/runner/deploy.sh --build-node
#     - bash $HOME/runner/deploy.sh --build-java
#     - bash $HOME/runner/deploy.sh --build-python
#     - bash $HOME/runner/deploy.sh --build-docker
#   when: manual
#   only:
#     - develop
#     - testing
#     - release
#     - master

deploy:
  stage: deploy
  # artifacts:
  #   untracked: true
  #   when: on_failure
  #   expire_in: 3 days
  #   paths:
  #     - target/
  #     - dist/
  script:
    - bash $HOME/runner/deploy.sh
  environment:
    name: ENV-${CI_COMMIT_REF_NAME}
  only:
    - dev
    - develop
    - test
    - sit
    - testing
    - uat
    - release
    - master
    - main
  interruptible: true

# test_function:
#   stage: test_function
#   script:
#     - bash $HOME/runner/deploy.sh --test-function
#   when: manual
#   only:
#     - dev
#     - develop
#     - test
#     - sit
#     - testing
#     - uat
#     - release
