# .gitea/workflows/ci.yaml
name: Gitea Actions
run-name: ${{ gitea.actor }} is Gitea Actions 🚀
on:
  push:
    branches:
      - develop
      - dev
      - sit
      - testing
      - uat
      - release
      - main
      - master
      - prod
  pull_request:
    branches:
      - develop
      - dev
      - sit
      - testing
      - uat
      - release
      - main
      - master
      - prod
# on:
#   schedule:
#     - cron: '30 5 * * 1,3'
#     - cron: '30 5 * * 2,4'

jobs:
  Single-Deploy-Job:
    runs-on: my_host
    steps:
      - name: deploy.sh
        run: $HOME/runner/deploy.sh
