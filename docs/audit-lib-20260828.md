# lib/ 审核报告（2026-08-28）

审核范围: `deploy.sh/lib/*.sh` 全部 11 个模块, 共 7069 行。
方式: shellcheck 全量 + 4 路并行逻辑审核 + 高危项逐一实证抽验
（set -e 复现、调用链核对、条件赋值路径验证）。

> 状态标记: ✅ 已修复 / 📝 待修复 / ✔ 经核实非 bug

## 总体结论

- shellcheck `-S warning` 全绿, bash -n 全过。
- 系统性问题只有一类, 但分布极广（约 20 处）:
  **`set -Eeo pipefail` 下手写的错误处理退化为死代码** ——
  `var=$(curl|grep|jq ...)` 后跟 `if [ -z ... ]` 判断, 赋值失败时 errexit
  先杀脚本, 后面的错误分支与回退永不执行。
  触发面: grep 无匹配、jq 解析失败、API 限流/不可达、空仓库。
- 次生模式: `|| _msg error` 吞退出码（`_msg` 恒 return 0）、后台 `&` 吞退出码。

## 高危

| # | 位置 | 问题 | 状态 |
|---|------|------|------|
| 1 | common.sh:1281-1313 | `release_info=$(curl ...)` 后 `local ret=$?` 判断; 1312 grep 无匹配同族。curl 失败/资产名不含 os+arch 时赋值即被 errexit 杀死, master.tar.gz 回退与 1314 的 if 分支全是死代码 | ✅ |
| 2 | analysis.sh:48-60 | ZAP 容器输出 `report.html`（`-r report.html`）, mv 源却凭空构造 `zap_report_<ts>.html`; 扫描"成功"必 mv 失败被 set -e 杀, 连错误提示都没有 | ✅ |
| 3 | repo.sh:196,234,275 等 6 处 | `version=$(grep|sed|head)` + pipefail: grep 无匹配即整体退出（已实测复现），调用方是顶层裸调用 | ✅ |
| 4 | deployment.sh:75-82,919 | `_project_oss_dest` 对空/坏 `G_CONF` 时 `jq ... 2>/dev/null` exit 2 → errexit 中止, ENV_OSS_DEST 兜底不可达 | ✅ |
| 5 | deployment.sh:900 + system.sh:455 | detect_deployment_method 用命令替换捕获 stdout; 全链 `_msg` 均 `>&2`, 唯独 check_k8s_available 的 warn 未重定向 → kubectl 有/helm 无时 type 被污染成多行, case 误入 `*` 报 Unknown method | ✅ |
| 6 | test.sh:310/329/348 | 三个 stage 入口各自 `G_TEST_RESULT=0`（auto 模式三个全跑）→ unit 失败被后阶段清零, 通知误报全绿 | ✅ |

## 中危（待修）

| # | 位置 | 问题 |
|---|------|------|
| M1 | common.sh:1208 | `dmesg|grep` 无匹配 → pipefail 非零, 诊断函数成定时炸弹 |
| M2 | build.sh:27,50 | `|| _msg error` 吞退出码, buildx 节点建失败仍继续, 故障延后且误导 |
| M3 | common.sh:955 | sudo 清空变量, HELM_INSTALL_DIR 丢失 → helm 装错目录 |
| M4 | common.sh:1090 | `pipx install matrix-nio` 恒失败（纯库无 CLI, 注释自证）|
| M5 | deployment.sh:301 | `helm ... >/dev/null || return 1` 吞输出且跳过回滚/诊断提示块 |
| M6 | deployment.sh:46 | `source` 项目钩子失败 → errexit 杀全流水线, handle_notify 被跳 |
| M7 | config.sh:49 | `command -v jq || _install_packages jq` 失败即死, 错误提示死码 |
| M8 | analysis.sh:288-291 | codeclimate 退出码 1=发现 issue 被当失败提前 return, 报告生成段不可达 |
| M9 | analysis.sh:85 | `source config.cfg` 无存在性守卫, --dry 也绕不过 |
| M10 | style.sh:186-187 | `$sc && shellcheck || exit_code=$?`: sc=false 时错误门控 |
| M11 | style.sh:20 | `git diff HEAD` 在 CI 干净 worktree 恒空, PHP 检查永远空转 |
| M12 | deployment.sh:857 | G_NAMESPACE 内联插值进 jq 程序（应 --arg）|
| M13 | deployment.sh:1126-1132 | clean_old_tags 全部 `&` 后台化 + `exit $?` 取 sleep 状态恒 0 |
| M14 | kubernetes.sh:239,59 | pv_name/sc_name/ports 缺 local 全局泄漏 |
| M15 | notify.sh | webhook curl 无 --fail + 消息未 JSON 转义, 4xx 也报成功 |

## 低危（待修）

- repo.sh:525 symbolic-ref 失败死码; :357 macOS 无 md5sum; :128 rsync_opts 拆词 glob
- deployment.sh:465 `-t` 与 `--no-times` 矛盾; :171 grep 正则元字符; :132 cleanup `&` 竞态
- common.sh:435 macOS IPv6 `%en0` 尾缀; :482 apt 漏 `-y`; :798 sha256 curl 无守卫
- build.sh:187 sed 子串替换破坏 HCL; :487 uuidgen 缺失即死; :501 rmi 后台竞态
- kubernetes.sh:140 GNU sed 语法; :456 fzf 空选择报成功
- notify.sh:119 git log 无提交 exit 128 杀通知

## 复核结论（✔ 非 bug）

- kubernetes.sh:229/302 `exit $?`: if 空分支后 $? 被复位为 0, 属与 CNFS 创建场景
  有关的问题但经实测非普遍误报, 降为中危观察项。
- system.sh:82 `docker image ls` 空过滤: ENV_DOCKER_REGISTRY 经 config_repo_vars
  必有默认值, 仅独立调用/显式置空时才会列全量, 降为低危观察项。

## 修复约定（高危已用, 中危沿用）

1. 命令替换必须兜底: `var=$(cmd ...) || true` / 放进 `if !`。
2. `&&` 列表末尾加 `|| true`, 避免 errexit 豁免仅在 `||` 前失效。
3. `_msg` 无错误时一律 `>&2`（检测链 stdout 只能有业务返回值）。
4. 布尔门控判断用 `[[ ... == true ]]`, 不做字符串当命令。
5. 失败即 fail-fast: `|| { _msg error; return 1; }`, 不吞码、不后台化。